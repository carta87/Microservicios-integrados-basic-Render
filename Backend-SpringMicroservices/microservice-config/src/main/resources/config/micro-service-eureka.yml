server:
  port: 8761

spring:
  application:
    name: micro-service-eureka

eureka:
  instance:
    hostname: localhost   # 👈 Eureka funciona localmente dentro del contenedor
    preferIpAddress: true
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: https://microservice-eureka-v1.onrender.com/eureka/