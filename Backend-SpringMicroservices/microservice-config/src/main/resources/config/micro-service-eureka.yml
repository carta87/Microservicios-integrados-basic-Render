server:
  port: 8761

spring:
  application:
    name: micro-service-eureka

eureka:
  instance:
    hostname: ${RENDER_EXTERNAL_URL}   # 👉 URL pública automática de Render
  client:
    register-with-eureka: false        # Un servidor Eureka NO se registra a sí mismo
    fetch-registry: false
    service-url:
      defaultZone: ${RENDER_EXTERNAL_URL}/eureka/   # 👉 URL pública correcta